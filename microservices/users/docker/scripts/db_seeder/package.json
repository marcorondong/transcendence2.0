{
  "name": "scripts",
  "version": "1.0.1",
  "main": "index.js",
  "scripts": {
    "seed": "ts-node user-seeder.ts",
    "seed:users:auto": "npm run seed -- --model=user --auto",
    "seed:users:file": "npm run seed -- --model=user --file=./seed_data/users_seeded.csv",
    "seed:users:seed": "npm run seed -- --model=user --auto --seed=871160",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Database seeder",
  "devDependencies": {
    "@faker-js/faker": "^9.8.0",
    "axios": "^1.9.0",
    "inquirer": "^12.6.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
